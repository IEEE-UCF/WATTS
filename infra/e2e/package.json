{
	"name": "@watts/e2e",
	"version": "0.0.0",
	"private": true,
	"description": "Playwright E2E smoke suite for the WATTS website — dev/validation only, not shipped.",
	"type": "module",
	"scripts": {
		"test": "playwright test",
		"test:ui": "playwright test --ui",
		"auth": "node capture-auth.mjs",
		"role-restore": "node restore-role.mjs",
		"session-clean": "node session-clean.mjs",
		"lint": "eslint .",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@aws-sdk/client-s3": "^3.1127.0",
		"@watts/config": "workspace:*",
		"pg": "8.23.0"
	},
	"devDependencies": {
		"@playwright/test": "^1.50.0",
		"@types/node": "^20.19.32",
		"@types/pg": "^8.16.0",
		"@watts/eslint-config": "workspace:*",
		"@watts/tsconfig": "workspace:*",
		"eslint": "^10.10.0",
		"pdf-lib": "^1.17.1"
	}
}
