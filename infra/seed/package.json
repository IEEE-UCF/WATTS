{
  "name": "@watts/seed",
  "version": "0.0.0",
  "private": true,
  "description": "Local database seeding — dev admin + (stale, unwired) domain fixtures.",
  "type": "module",
  "scripts": {
    "seed": "tsx src/seed.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@watts/config": "workspace:*",
    "@watts/db": "workspace:*",
    "drizzle-orm": "0.45.2",
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "@watts/eslint-config": "workspace:*",
    "@watts/tsconfig": "workspace:*",
    "eslint": "^10.10.0",
    "tsx": "^4.19.2"
  }
}
