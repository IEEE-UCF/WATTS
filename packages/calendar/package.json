{
  "name": "@watts/calendar",
  "version": "0.0.0",
  "private": true,
  "description": "Google Calendar sync client for WATTS — service-account auth, event CRUD, label→colorId mapping. Framework-neutral: no next / react / @trpc / drizzle.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@watts/config": "workspace:*",
    "google-auth-library": "^9.15.1"
  },
  "devDependencies": {
    "@watts/eslint-config": "workspace:*",
    "@watts/tsconfig": "workspace:*",
    "eslint": "^10.10.0"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  }
}
